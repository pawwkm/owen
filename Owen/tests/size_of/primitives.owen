namespace Test

function main
    output I32

    assert size of I8 == 1
    assert size of I16 == 2
    assert size of I32 == 4
    assert size of I64 == 8

    assert size of U8 == 1
    assert size of U16 == 2
    assert size of U32 == 4
    assert size of U64 == 8

    return 0
end