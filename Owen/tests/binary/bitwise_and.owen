namespace Bitwise_And

enumeration Direction of U8
    North
    South
    East
    West
end

function main
    output I32

    assert 1i8 & 2 == 0
    assert 1i16 & 2 == 0
    assert 1i64 & 2 == 0
    assert 1u8 & 2 == 0
    assert 1u16 & 2 == 0
    assert 1u32 & 2 == 0
    assert 1u64 & 2 == 0
    assert Direction.South & Direction.East == Direction.North

    return 0
end