// expect 0

namespace Test

ctfe main()

external function malloc
    generalize T
    input U64 amount
    output *T
    "msvcr100.dll"

external function free
    generalize T
    input *T data
    "msvcr100.dll"

function main 
    output I32
    
    a = malloc<I32>(16)
    b = malloc<I16>(16)

    free(a)
    free(b)

    return 0
end