// expect 0

namespace Binary_Operator_Precedence

function main
    output I32
    
    assert true || true && false
    assert true && false == false
    assert true && false != true
    assert true && 0i32 <= 1
    assert true && 1i32 >= 0
    assert true && 0i32 < 1
    assert true && 1i32 > 0

    return 0
end